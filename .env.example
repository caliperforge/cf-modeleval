# cf-modeleval-v1 — copy to .env (gitignored) and paste real keys for live runs.
# Anthropic is required (target + judge); OpenAI/Groq are required for cross-provider receipts.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GROQ_API_KEY=
